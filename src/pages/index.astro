<!-- ABOUTME: Introduces FoxLift with a direct product explanation, origin story, and measured results. -->
<!-- ABOUTME: Launch landing page: every number is sourced from the repo's receipts and status docs. -->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#101212" />
    <meta
      name="description"
      content="FoxLift is an open-source Visual FoxPro 9 decompiler. Compiled binaries in, functionally equivalent and rebuildable projects out."
    />
    <meta name="robots" content="index, follow" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="FoxLift" />
    <meta property="og:title" content="FoxLift | Visual FoxPro 9 decompiler" />
    <meta
      property="og:description"
      content="Compiled Visual FoxPro 9 binaries in. Functionally equivalent, rebuildable projects out."
    />
    <meta property="og:url" content="https://foxlift.dev/" />
    <meta property="og:image" content="https://foxlift.dev/images/foxlift-og.webp" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="FoxLift | Visual FoxPro 9 decompiler" />
    <meta
      name="twitter:description"
      content="Compiled Visual FoxPro 9 binaries in. Functionally equivalent, rebuildable projects out."
    />
    <meta name="twitter:image" content="https://foxlift.dev/images/foxlift-og.webp" />
    <link rel="canonical" href="https://foxlift.dev/" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <title>FoxLift | Visual FoxPro 9 decompiler</title>
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>

    <header class="site-header">
      <nav class="shell nav" aria-label="Main navigation">
        <a class="brand" href="#top" aria-label="FoxLift home">
          <img src="/images/foxlift-mark.webp" width="42" height="42" alt="" fetchpriority="high" />
          <span>FoxLift</span>
        </a>
        <div class="nav-side">
          <a class="nav-link" href="/about/">About</a>
          <p class="release-note"><span aria-hidden="true"></span><a href="https://github.com/foxlift/foxlift">Open source on GitHub</a></p>
        </div>
      </nav>
    </header>

    <main id="main-content">
      <section class="hero" id="top">
        <div class="shell hero-grid">
          <div class="hero-copy">
            <h1>Visual FoxPro&nbsp;9 decompiler.</h1>
            <p class="hero-summary">
              Compiled VFP9 binaries in. Functionally equivalent, rebuildable projects out.
            </p>
          </div>

          <dl class="hero-facts" aria-label="FoxLift facts">
            <div>
              <dt>10,514 / 10,514</dt>
              <dd>sections lifted, development corpus</dd>
            </div>
            <div>
              <dt>94.9%</dt>
              <dd>sections lifted, held-out repos</dd>
            </div>
            <div>
              <dt>Offline</dt>
              <dd>for pure decompilation</dd>
            </div>
          </dl>
        </div>
      </section>

      <section class="section story" id="quickstart">
        <div class="shell story-grid">
          <div class="story-copy" data-reveal>
            <h2>Two commands.</h2>
            <p class="lead">
              Install from PyPI, point it at a compiled binary — .exe, .app, .fxp, .scx, .vcx — and read the verdict. No FoxPro installation needed to decompile.
            </p>

            <pre class="code-panel"><code>pip install foxlift

foxlift inspect erp_client.exe            # what is inside
foxlift decompile erp_client.exe -o recovered/
&#123;
  "outdir": "recovered/",
  "modules": 13,
  "verified": false                       # exit 2 — a partial, honestly marked
&#125;</code></pre>

            <p>What lands on disk:</p>

            <pre class="code-panel"><code>recovered/
├── module_000e00/
│   ├── source.prg      # canonical VFP source
│   ├── symbols.txt     # recovered symbol table
│   └── meta.json       # per-section verdicts, with reasons
└── module_005e00/ …</code></pre>

            <p>And inside a <code>source.prg</code>:</p>

            <pre class="code-panel"><code>* --- section 1 ---
LPARAMETERS TCORDERNO
SELECT ORDERS
LOCATE FOR ORDER_NO = TCORDERNO
THIS.PARENT.TXTCUSTOMER.VALUE = ORDERS.CUST_NAME
THIS.PARENT.CDSAVE.ENABLED = .T.
MESSAGEBOX("Loaded " + TCORDERNO, 64, "Orders")
* --- section 2 (1 statement(s) beyond the supported slice: SET variant outside forced subset) ---</code></pre>

            <p>
              Abridged, but true to shape: every liftable statement comes back as canonical VFP, and anything beyond the measured slice stays in the output as an annotated placeholder naming its exact blocker. Nothing is silently dropped.
            </p>
          </div>
        </div>
      </section>

      <section class="section process" id="process">
        <div class="shell process-grid">
          <div class="process-heading" data-reveal>
            <h2>What it does.</h2>
            <p>FoxLift follows a measured path from binary containers back to buildable VFP projects. The standard is simple: recovered projects must rebuild and behave like the compiled input — readable guesses are not enough.</p>
          </div>

          <ol class="process-list" data-reveal>
            <li>
              <span>01</span>
              <div><h3>Read</h3><p>Find modules, forms, classes, reports, menus, and resources.</p></div>
            </li>
            <li>
              <span>02</span>
              <div><h3>Lift</h3><p>Decode measured bytecode, symbols, expressions, and control flow.</p></div>
            </li>
            <li>
              <span>03</span>
              <div><h3>Write</h3><p>Emit functionally equivalent VFP source and project artifacts.</p></div>
            </li>
            <li>
              <span>04</span>
              <div><h3>Verify</h3><p>Rebuild in VFP9 and compare canonical bytecode.</p></div>
            </li>
          </ol>
        </div>
      </section>

      <section class="section story" id="receipts">
        <div class="shell story-grid">
          <div class="story-copy" data-reveal>
            <h2>Round-trip, with receipts.</h2>
            <p class="lead">
              The repo ships a small authored demo project. Compile it on VFP9, decompile it with the CLI, recompile the output: statement frames and symbol tables come back byte-identical. Exit codes, hashes, and per-section verdicts are committed alongside it.
            </p>
            <p>
              Every benchmark corpus is public VFP code on GitHub. On the development corpus — <a href="https://github.com/luhongbin/YiFeiERP">luhongbin/YiFeiERP</a>, one vendor's real ERP estate of forms and class libraries — 10,514 of 10,514 method sections lift, none blocked, none silently dropped. On repositories never used to infer schemas — <a href="https://github.com/VFPX/AATest">VFPX/AATest</a>, <a href="https://github.com/RubenAlejandroFalcon/ARoMal">ARoMal</a>, and <a href="https://github.com/VFPX/ReportingApps">VFPX/ReportingApps</a> for validation, plus held-out repos such as <a href="https://github.com/ankuryu/vfpObjbill">vfpObjbill</a> opened once per round and never tuned on — 95.1% of validation sections and 94.9% of held-out sections lift. Recompiling decompiled compiled programs on a real VFP9 reproduces the original bytecode frames for 95% of the measured population.
            </p>
            <p>
              Some things provably cannot come back: comments are never compiled, DECLARE parameter names are discarded, WAIT WINDOW clause order is canonicalised by the compiler, and some identifiers are blanked by the compiler itself. FoxLift proves what is unrecoverable and never invents it.
            </p>
            <a class="button" href="https://github.com/foxlift/foxlift/tree/main/demo">See the demo receipts</a>
          </div>
        </div>
      </section>

      <section class="section story" id="status">
        <div class="shell story-grid">
          <div class="story-copy" data-reveal>
            <h2>Where we stand.</h2>
            <p class="lead">
              Method code comes back today; whole rebuildable projects are the roadmap. In between, partial results are the contract, not a failure mode.
            </p>

            <ol class="timeline" aria-label="Project status timeline">
              <li>
                <p class="timeline-date">Aug 27</p>
                <div class="timeline-body">
                  <h3>v0.1.0 — first public release</h3>
                  <p>
                    Forms and class libraries (.scx/.vcx), compiled programs (.fxp), and module
                    scans of .app/.exe decompile to per-module VFP source with symbols and
                    per-section verdicts. Round-trip demo with committed
                    receipts. On <a href="https://pypi.org/project/foxlift/">PyPI</a> and <a href="https://github.com/foxlift/foxlift">GitHub</a>.
                  </p>
                </div>
              </li>
              <li>
                <p class="timeline-date">Aug 28</p>
                <div class="timeline-body">
                  <h3>v0.2.0 — compiled programs and menus</h3>
                  <p>
                    DEFINE CLASS scaffolds in .fxp, compiled menu programs (.mpx), SQL decode, and
                    non-Latin code pages. Recompiling the decompiled output on real VFP9
                    reproduces the original bytecode frames for 95% of the measured population.
                  </p>
                </div>
              </li>
              <li data-now>
                <p class="timeline-date">Now</p>
                <div class="timeline-body">
                  <h3>Currently working on: artifact reconstruction</h3>
                  <p>
                    Writing the binaries back. The table writer already regenerates all 522
                    development .scx/.vcx/.mnx/.dbf files byte-identical, reconstructed forms
                    recompile on real VFP9 with 98.2% of method sections frame-equal, and a
                    writer-generated .pjx project has linked into a byte-identical .app on the
                    compiler rig — fully headless. Shipping that loop end to end — decompile,
                    regenerate, BUILD APP — is the work in flight.
                  </p>
                </div>
              </li>
              <li>
                <p class="timeline-date">Next</p>
                <div class="timeline-body">
                  <h3>Whole applications</h3>
                  <p>
                    Point FoxLift at an .exe or .app and get back a project that rebuilds and runs
                    in VFP9 — reports and the remaining artifact types close out the set.
                  </p>
                </div>
              </li>
            </ol>

            <p>
              Hand FoxLift any binary and it tells the truth about it. A file that is not VFP is refused cleanly. A VFP binary always yields whatever is provable: every module found is decompiled, and any statement beyond the measured slice stays in the output as an annotated placeholder naming the exact blocker — never silently dropped. Exit 0 with a verified verdict means a 100% lift; exit 2 marks a partial.
            </p>
            <p>
              Concretely: a real production .exe from the development corpus currently comes back as 13 modules and roughly 10,900 lines of source — 159 of 160 sections lifted, and the one miss is labeled in place with the unmeasured SET variant that blocks it.
            </p>
            <p>
              Scope is Visual FoxPro 9 (SP2). Every claim above is measured against a real VFP9 compiler; nothing is guessed.
            </p>
          </div>
        </div>
      </section>

      <section class="section story how-teaser" id="how">
        <div class="shell story-grid">
          <div class="story-copy" data-reveal>
            <h2>How we built it.</h2>
            <p class="lead">
              A weekend spike, then a six-day measured campaign: 3.7&#8202;billion tokens through a
              fleet of AI workers, a loop of overseers, and a real VFP9 compiler as the referee.
            </p>
            <p>
              No finding survived on model consensus — every hypothesis was settled by compiling an
              authored probe on a real VFP9 and comparing the bytecode. The full story, with the
              campaign numbers, is on its own page.
            </p>
            <a class="button" href="/about/">Read how it was built</a>
          </div>
        </div>
      </section>
    </main>

    <footer class="site-footer">
      <div class="shell footer-inner">
        <a class="brand footer-brand" href="#top" aria-label="FoxLift home">
          <img src="/images/foxlift-mark.webp" width="42" height="42" alt="" />
          <span>FoxLift</span>
        </a>
        <p>Created by <a href="https://github.com/timo-bejan">timo-bejan</a> · Open source under AGPL-3.0 — <a href="https://github.com/foxlift/foxlift">github.com/foxlift/foxlift</a></p>
      </div>
    </footer>

    <script src="../scripts/site.ts"></script>
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
